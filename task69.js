function testLogicalAnd(val) {
    // Змініть код лише під цим рядком

    if (val <= 50 && val >= 25) {
         {
            return "Yes";
        }
    }

    // Змініть код лише над цим рядком
    return "No";
}

testLogicalAnd(10);
